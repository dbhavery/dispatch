/**
 * Barrel re-export for @dispatch/shared/contexts mock.
 * Mirrors the real module's public API surface used by portal code.
 */

export { useAuth, __setMockAuthValues, __resetMockAuth } from './AuthContext';
